import { AuditColumns } from '../audit/audit-columns';
export declare class TicketingSales extends AuditColumns {
    performanceId: number;
    salesDate: string;
    performanceSalesQuantity: number | null;
    performanceSalesRevenue: number | null;
}

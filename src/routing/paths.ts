/** Route prefixes for the EMS app (view shell at `/`). */
export const EMS_ROOT = "/";

/** Company Hub routes (same deploy as EMS by default). */
export const INTERNAL_ROOT = "/internal";

/** Post-login destination when no deep link is provided. */
export const APP_CHOOSER_PATH = "/apps";

export const LOGIN_PATH = "/login";

export const INTERNAL_HOME_PATH = INTERNAL_ROOT;
